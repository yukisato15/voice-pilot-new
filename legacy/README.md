# Legacy Assets

このディレクトリは旧 UI や検証コードを退避するための保管場所です。現行アプリのビルドには含まれないため、参照のみを目的として利用してください。

- 旧 `renderer/` / `main/` を移す場合は、`legacy/renderer_legacy/` や `legacy/main_legacy/` のようにサブディレクトリを切って整理してください。
- 新 UI への移植が完了したら、対応するレガシーコードをアーカイブするか削除していきます。
